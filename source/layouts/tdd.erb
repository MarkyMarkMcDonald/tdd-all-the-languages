<% wrap_layout :layout do %>
  <h1>TDD with <%= current_page.data.language %></h1>

  <h2>Installing the Language</h2>

  <p>The first thing we need to do is install the language on our system.  We'll be using Homebrew to do that.</p>

  <%= partial "language/" + current_page.data.language.downcase + "/language_installation" %>

  <h2>Creating a Project</h2>

  <%= partial "language/" + current_page.data.language.downcase + "/project_setup" %>

  <h2>Add Unit Testing Library</h2>

  <%= partial "language/" + current_page.data.language.downcase + "/testing_library" %>

  <h2>Our First Failing Test</h2>

  <%= partial "language/" + current_page.data.language.downcase + "/first_test" %>
<% end %>
